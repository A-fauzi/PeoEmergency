<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.afauzi.peoemergency">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PeoEmergency">
        <activity
            android:name=".localStorage.activity.EditNoteActivity"
            android:exported="false" />
        <activity
            android:name=".localStorage.activity.NoteActivity"
            android:exported="true">
        </activity>
        <activity
            android:name=".screen.main.fragment.activity.accountProfile.EditProfileActivity"
            android:exported="false" />
        <activity
            android:name=".screen.auth.SignUpActivity"
            android:exported="false" />
        <activity
            android:name=".screen.auth.SignInActivity"
            android:exported="false" />
        <activity
            android:name=".screen.LandingActivity"
            android:exported="true"
            android:theme="@style/Theme.MySplash">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".screen.main.MainActivity"
            android:exported="true" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>